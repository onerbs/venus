module main

import xsh

fn play() ? {
	this_file := '${xsh.cmd_name}.vv'
	println(xsh.read_file(this_file) ?)
	println('--- ✨ ---')
	for line in xsh.read_lines(this_file) ? {
		println(line)
	}
}

fn main() {
	play() or { xsh.report(err) }
}
